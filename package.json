{
  "name": "pvms",
  "version": "1.0.0",
  "description": "Public Violation Management System — municipal property violation reporting and enforcement portal",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed:demo": "npm run seed:demo --prefix backend",
    "db:import": "npm run db:import --prefix backend",
    "db:up": "docker compose -f database/docker-compose.yml up -d",
    "db:down": "docker compose -f database/docker-compose.yml down",
    "test:api": "npm run test:api --prefix backend",
    "build:frontend": "npm run build --prefix frontend"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Ajinkya1835/PVMS-Public-Violation-Management-System.git"
  },
  "keywords": [
    "pvms",
    "violation-management",
    "municipal",
    "react",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "MIT"
}
